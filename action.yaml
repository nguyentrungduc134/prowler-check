name: Compliance check

on:
  workflow_call:  
    inputs:
      config-path:
        required: true
        type: string
env:
  applicationfolder: core



jobs:
  configure:
      - name: Print the input name to STDOUT
        run: echo The username is ${{ inputs.config-path }}
